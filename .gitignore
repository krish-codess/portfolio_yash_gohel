# dependencies
node_modules/
admin/node_modules/
backend/node_modules/

# env / secrets
.env
.env.local
.env.*.local
admin/.env
backend/.env
*.token_cache
ytmusic_auth.json

# personal local tooling, not site content — never belongs in this repo.
# yt_to_spotify_sync.py has a hardcoded Spotify client secret, curl.txt and
# ytmusic_auth.json carry live YouTube session cookies/auth headers, and
# .spotify_token_cache is an OAuth token cache. Excluded as a whole folder
# rather than per-file so nothing new dropped in here leaks by accident.
AI Projects/

# stray local python venv
.venv/

# OS / editor
.DS_Store
Thumbs.db

# build artifacts (no bundler exists today, harmless if unused)
dist/
build/
.netlify/
